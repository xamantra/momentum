name: momentum
description: MVC pattern for flutter. Works as state management, dependency injection and service locator.
version: 2.1.1
homepage: https://github.com/xamantra/momentum

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  collection: ^1.15.0
dev_dependencies:
  flutter_test:
    sdk: flutter

  math_expressions: ^2.1.0-nullsafety.0
  dio: ^4.0.0-beta7
  relative_scale: ^2.0.0
